import React from "react";
import { Text, View } from "react-native";
import "../../global.css";

const MainScreen = () => {
  return (
    <View className="bg-white items-center">
      <Text className="text-xl">메인화면입니다.</Text>
    </View>
  );
};

export default MainScreen;
